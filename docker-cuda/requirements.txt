# Mirrors the pip section of environment.yml.
#
# NOTE: `imitation==1.0.0` is deliberately NOT listed here. It must be installed in a
# second pip pass (see the Dockerfile and the README), because pip cannot resolve it
# together with gym:
#   MyoSuite 2.2.0 pins gym==0.13, gym 0.13.0 declares cloudpickle~=1.2.0,
#   imitation 1.0.0 -> huggingface-sb3 3.0 declares cloudpickle>=1.6.
# Installing imitation afterwards upgrades cloudpickle to 3.x, which is what every
# package actually runs against; only gym's (unused) stale pin is left unsatisfied.

cloudpickle==1.2.2
gym==0.13.0
gymnasium==0.29.1
h5py==3.7.0
wandb
tqdm
numpy
ipdb
stable-baselines3==2.2.1
MyoSuite==2.2.0
sb3-contrib==2.2.1
Shimmy==1.3.0
imageio
# Analysis and plotting
matplotlib
seaborn
scikit-learn
scipy
pandas
tensorboard
joblib
